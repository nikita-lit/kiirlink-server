services:
  api:
    build: .
    container_name: kiirlink-backend
    ports:
      - "80:8080"
    volumes:
      - ./data:/app/data
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    user: "1654:1654"
    restart: always